<div class="container">
  <div class="card mb-5">
    <img src="<%= @flat["imageUrl"] %>" alt="<%= @flat["name"] %>" class="card-img-top">
    <div class="card-body">
      <h1 class="card-title"><%= @flat["name"] %></h1>
      <h2 class="card-text">$<%= @flat["price"]  %></h2>
    </div>
  </div>
  <%= link_to "Home", root_path, class: 'btn btn-primary' %>
</div>
